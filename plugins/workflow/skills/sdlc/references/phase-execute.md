# EXECUTE Phase

`sdlc-execute` owns this Phase inside the EXECUTE Stage. Apply the current plan through bounded work and deterministic sensors. Use TDD when production behavior and tests are in scope. Return a compact execution result with verified patch, log, or report Artifact References on the completed Lifecycle.

A corrective EXECUTE Phase does not modify earlier work. It is appended after REVIEW and must be followed by a fresh REVIEW before SHIP.
