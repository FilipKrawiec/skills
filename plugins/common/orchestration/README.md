# Provider-Neutral Orchestration

This active plugin provides the portable, code-free `orchestrate-delivery`, `scaffold-monorepo`, and `define` skills. Host adapters and executor-control instructions are intentionally outside this package.

